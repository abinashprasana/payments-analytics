# Legacy chart exports

These images were produced by the retired v1 exporter, which contained a second set of analytical SQL and mixed currencies in labels. They are retained only for visual provenance and are not consumed by either v2 public surface.

V2 artifacts are generated from the canonical SQL marts through `scripts/generate_artifacts.py`.
